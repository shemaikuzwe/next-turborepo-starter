services:
 
  client:
    build: 
      context: .
      dockerfile: client.Dockerfile
      args:
        - DATABASE_URL=postgresql://next-starter_owner:npg_RPOtK2GLJpQ0@ep-lingering-flower-a8clhjib-pooler.eastus2.azure.neon.tech/next-starter?sslmode=require
    ports:
      - "3001:3000"
    restart: unless-stopped
    # env_file:
    #   - ./apps/client/.env
    # depends_on:
    #   - postgres
    environment:
      - NODE_ENV=PRODUCTION
      - DATABASE_URL=${DATABASE_URL}

  admin:
    build: 
      context: .
      dockerfile: admin.Dockerfile
      args:
        - DATABASE_URL=${DATABASE_URL}
    ports:
      - "3000:3000"
    restart: unless-stopped
    # env_file:
    #   - ./apps/admin/.env
    # depends_on:
    #   - postgres
    environment:
      - NODE_ENV=PRODUCTION
      - DATABASE_URL=${DATABASE_URL}

  # postgres:
  #   image: postgres:14-alpine
  #   ports:
  #     - "555:5432"
  #   environment:
  #     POSTGRES_PASSWORD: "1234"
  #   volumes:
  #     - postgres-data:/var/lib/postgresql/data 
  #   restart: unless-stopped
   
  #   healthcheck:
  #     test: ["CMD", "pg_isready", "-U", "postgres"]
  #     interval: 10s
  #     timeout: 5s
  #     retries: 5   

# volumes:
#   postgres-data: